apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: buva-product-images
  namespace: buva
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
